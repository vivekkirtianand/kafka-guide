import { ConfigEntry } from "@/lib/types";

export const configs: ConfigEntry[] = [
  {
    key: "bootstrap.servers",
    exampleValue: "broker1.example.com:9092,broker2.example.com:9092,broker3.example.com:9092",
    safeBaseline: "Two or three brokers, or one load-balanced endpoint, taken verbatim from your platform's connection string.",
    verification: "The client logs a cluster id and a full broker list at startup, and a metadata request in a debug log returns every broker rather than only the ones listed here.",
    rollback: "Restore the previous list and recreate the client — there is no persisted state to clean up.",
    managedCaveat: "The provider gives you the exact bootstrap string (often one hostname that load-balances across brokers); do not shorten or reorder it.",
    scope: "client",
    goal: "Connect to the cluster",
    controls:
      "The initial list of host:port broker addresses a client contacts on startup to discover the rest of the cluster. It does not need every broker — one reachable broker is enough to learn the full set.",
    defaultValue: '"" (required — a client will not start without it)',
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Set it once from the bootstrap string your platform or cluster gives you. List two or three brokers (or a load-balanced endpoint) so a single down broker at startup does not stop a client from bootstrapping.",
    performanceImpact:
      "No steady-state effect — it is used for the first metadata fetch and to re-bootstrap if every known broker becomes unreachable.",
    reliabilityImpact:
      "If every address here is unreachable when the client starts, it never connects. Once running, the client uses the complete broker list it discovered, so a listed broker failing later is harmless.",
    relatedConfigs: ["security.protocol", "metadata.max.age.ms", "advertised.listeners"],
    failureModes: [
      "Only one broker listed and it is the one that is down at startup — the client fails to bootstrap even though the cluster is healthy",
      "Addresses reachable from a developer laptop but not from production, so it works locally and times out once deployed",
    ],
  },
  {
    key: "client.id",
    exampleValue: "order-service",
    safeBaseline: "A stable, human-readable application name — the same value on every instance of that application.",
    verification: "Broker request logs and the client's own producer/consumer metric tags carry this exact string.",
    scope: "client",
    goal: "Connect to the cluster",
    controls:
      "A free-form identifier the client sends with every request. It shows up in broker request logs, client-side metrics, and — together with the authenticated principal — in client quota matching. Left unset, the client generates one (producers use producer-<n>, consumers consumer-<group>-<n>), so it is never truly absent, just unstable and uninformative.",
    defaultValue: '"" (client generates producer-<n> / consumer-<group>-<n>)',
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Always set a stable, descriptive value per application (not per instance) so a noisy or misbehaving client is identifiable in broker logs and metrics without packet captures.",
    performanceImpact: "None.",
    reliabilityImpact:
      "No functional effect on its own. But the generated ids change on every restart and carry no application name, so attributing load, lag, or throttling to a specific service during an incident means correlating by host or principal instead.",
    relatedConfigs: ["bootstrap.servers", "group.id"],
    failureModes: [
      "Left unset across a fleet, so each instance reports a different auto-generated id and a client-id quota cannot be scoped to one application",
    ],
  },
  {
    key: "security.protocol",
    exampleValue: "SASL_SSL",
    safeBaseline: "SASL_SSL for any shared or production cluster; PLAINTEXT only for a local single-broker lab.",
    verification: "The client completes its first connection and a metadata fetch; a mismatch fails immediately with an SSL or SASL handshake error.",
    rollback: "Set it back to the previous value and recreate the client — a wrong value never partially connects, so nothing is half-applied.",
    managedCaveat: "Almost always SASL_SSL — the provider does not expose a PLAINTEXT listener.",
    scope: "client",
    goal: "Authenticate and encrypt",
    controls:
      "Which transport the client uses to reach brokers: PLAINTEXT (no auth, no encryption), SSL (TLS, optional mutual auth), SASL_PLAINTEXT (SASL auth, no encryption), or SASL_SSL (SASL auth over TLS).",
    defaultValue: "PLAINTEXT",
    changeMechanism: "recreate-client",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange:
      "Set it to match the listener you are connecting to. Managed services and most production clusters expose SASL_SSL; PLAINTEXT is only appropriate for a local single-broker lab.",
    performanceImpact:
      "TLS adds a handshake per connection and a few percent CPU for encryption; negligible for steady-state throughput on modern hardware.",
    reliabilityImpact:
      "A value that does not match the broker listener fails every connection immediately — there is no partial or degraded mode.",
    relatedConfigs: ["sasl.mechanism", "bootstrap.servers", "advertised.listeners"],
    failureModes: [
      "Client set to PLAINTEXT against a SASL_SSL listener (or vice versa) — connections are refused or hang with an opaque handshake error",
      "Connecting to the wrong advertised port for the chosen protocol",
    ],
  },
  {
    key: "sasl.mechanism",
    exampleValue: "SCRAM-SHA-256",
    safeBaseline: "Whatever the cluster enables — commonly SCRAM-SHA-256 or PLAIN, and only over SASL_SSL.",
    verification: "Authentication succeeds on the first connection; a wrong mechanism logs an unexpected-handshake or authentication-failed error.",
    managedCaveat: "The provider's docs name the mechanism; PLAIN and SCRAM-SHA-256 are the usual choices.",
    scope: "client",
    goal: "Authenticate and encrypt",
    controls:
      "Which SASL mechanism the client authenticates with when security.protocol is SASL_PLAINTEXT or SASL_SSL: commonly PLAIN, SCRAM-SHA-256, SCRAM-SHA-512, or OAUTHBEARER (GSSAPI for Kerberos).",
    defaultValue: "GSSAPI",
    changeMechanism: "recreate-client",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange:
      "Set it to the mechanism your cluster enables — SCRAM-SHA-256 or PLAIN for most managed services, OAUTHBEARER where token auth is in place. Only relevant alongside a SASL security.protocol.",
    performanceImpact: "None beyond the one-time authentication exchange when a connection is established.",
    reliabilityImpact:
      "A mechanism the broker has not enabled fails authentication on every connection. PLAIN over SASL_PLAINTEXT sends the password in cleartext — pair it only with SASL_SSL.",
    relatedConfigs: ["security.protocol"],
    failureModes: [
      "Choosing a mechanism the broker's sasl.enabled.mechanisms does not include — every client fails to authenticate",
      "PLAIN over SASL_PLAINTEXT, leaking credentials on the wire",
    ],
  },
  {
    key: "connections.max.idle.ms",
    exampleValue: "240000",
    safeBaseline: "Leave at 540000 unless a firewall or load balancer between the client and the brokers closes idle connections sooner.",
    verification: "\"Connection reset\" on the first request after a quiet period stops appearing in the client log.",
    rollback: "Restore the default and recreate the client.",
    scope: "client",
    goal: "Connect to the cluster",
    controls: "How long an idle connection to a broker is kept open before the client closes it.",
    defaultValue: "540000",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Lower it when a firewall or load balancer between the client and the brokers silently drops idle connections sooner than 9 minutes, so the client re-establishes them on its own schedule instead of discovering the drop mid-request.",
    performanceImpact:
      "Very short values cause needless reconnect churn; the reconnect itself costs a TCP and (with TLS/SASL) an auth round trip.",
    reliabilityImpact:
      "A middlebox idle timeout shorter than this value leads to periodic 'Connection reset' errors on the first request after a quiet period, usually retried transparently but noisy in logs.",
    relatedConfigs: ["metadata.max.age.ms", "reconnect.backoff.max.ms"],
    failureModes: [
      "Left at the default behind a load balancer with a 60s idle timeout — the first request after any lull fails with a connection reset",
    ],
  },
  {
    key: "reconnect.backoff.max.ms",
    safeBaseline: "Leave at 1000.",
    verification: "During a broker outage, reconnect attempts in a debug log space out geometrically up to this ceiling instead of at a fixed rate.",
    rollback: "Restore the default and recreate the client.",
    scope: "client",
    goal: "Connect to the cluster",
    controls:
      "The ceiling on the wait between reconnection attempts to a broker that keeps refusing connections. The wait starts at reconnect.backoff.ms (50 ms), grows exponentially with jitter, and is capped here.",
    defaultValue: "1000",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Rarely — raise it only if a large fleet reconnecting to a broker that just restarted produces a noticeable connection storm, to spread the retries out further.",
    performanceImpact:
      "Higher values reduce reconnect attempt rate against a down broker; they also lengthen the worst-case time to notice that broker has come back.",
    reliabilityImpact:
      "Does not affect a healthy connection. Only shapes how aggressively the client retries a broker it cannot currently reach.",
    relatedConfigs: ["retry.backoff.ms", "connections.max.idle.ms"],
    failureModes: [
      "Set very high, a client is slow to reconnect to a broker that recovered quickly, extending an outage that was already over",
    ],
  },
  {
    key: "retry.backoff.ms",
    safeBaseline: "Leave at 100 — the default already backs off exponentially.",
    rollback: "Restore the default and recreate the client.",
    scope: "client",
    goal: "Connect to the cluster",
    controls:
      "The initial wait before retrying a failed request to a specific topic-partition (a failed produce, a failed fetch). Since Kafka 3.7 it grows exponentially with jitter up to retry.backoff.max.ms (1000 ms).",
    defaultValue: "100",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Leave it alone in almost every case. Raising it slightly can help when retries are hammering a partition whose leader is mid-election.",
    performanceImpact:
      "Too low a value wastes the retry budget on tight-loop retries against a partition that needs a moment to settle; the default is already tuned for this.",
    reliabilityImpact:
      "Interacts with delivery.timeout.ms on the producer — every retry consumes part of that budget, so an aggressively low backoff can exhaust the retries before the partition recovers.",
    relatedConfigs: ["reconnect.backoff.max.ms", "retries", "delivery.timeout.ms"],
    failureModes: [
      "Lowered to a few milliseconds, a client generates a retry storm against a partition whose leader is briefly unavailable",
    ],
  },
  {
    key: "metadata.max.age.ms",
    exampleValue: "60000",
    safeBaseline: "Leave at 300000; lower to about 60000 only where brokers or partitions are added frequently.",
    verification: "After a partition is added, an idle client picks it up within the new interval without needing a produce or fetch error to force the refresh.",
    rollback: "Restore the default and recreate the client.",
    scope: "client",
    goal: "Connect to the cluster",
    controls:
      "How often the client proactively refreshes cluster metadata (which brokers exist, which broker leads each partition) even when nothing has visibly gone wrong. A leadership change or a request error also triggers an immediate refresh.",
    defaultValue: "300000",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Lower it when the broker set changes often (aggressive autoscaling, frequent rolling restarts) so clients pick up new brokers without waiting on an error to force the refresh.",
    performanceImpact:
      "Each refresh is one lightweight metadata request; even a 30s interval is negligible traffic for a normal client count.",
    reliabilityImpact:
      "Rarely the thing that catches a stale leader — the first request to a moved partition returns a leadership error that forces an immediate refresh anyway. Its real job is discovering brokers and partitions that were added while the client saw no error at all.",
    relatedConfigs: ["bootstrap.servers", "connections.max.idle.ms"],
    failureModes: [
      "Set very high on a cluster that scales out often — a client with no traffic to the new partitions never learns they exist until something forces a refresh",
    ],
  },
  {
    key: "acks",
    exampleValue: "all",
    safeBaseline: "all, paired with min.insync.replicas=2 on a replication-factor-3 topic.",
    verification: "Kill a broker mid-produce: with acks=all no acknowledged record is lost and the producer's record-error-rate stays flat.",
    rollback: "Set it back to the prior value and recreate the producer. Records already acknowledged under the weaker setting cannot be made durable retroactively.",
    managedCaveat: "Client-side; the provider enforces its own replication factor and often a minimum min.insync.replicas.",
    scope: "producer",
    goal: "Prevent acknowledged data loss",
    controls: "How many replicas must confirm a write before the producer considers it successful.",
    defaultValue: "all (Kafka 3.0+ clients)",
    changeMechanism: "recreate-client",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange: "Lower only for throughput-over-durability workloads (metrics, logs you can afford to lose).",
    performanceImpact: "acks=0 and acks=1 reduce latency; acks=all adds a round trip to the ISR.",
    reliabilityImpact: "acks=all is required for no-data-loss guarantees when paired with min.insync.replicas.",
    relatedConfigs: ["min.insync.replicas", "enable.idempotence", "retries"],
    failureModes: ["Silent data loss on leader failure when acks=0 or acks=1", "NOT_ENOUGH_REPLICAS with acks=all and a thin ISR"],
  },
  {
    key: "enable.idempotence",
    safeBaseline: "true — the default; do not turn it off on a modern cluster.",
    verification: "The producer's record-retry-rate can be non-zero while the topic shows no duplicated key/sequence at consumer offsets.",
    rollback: "Re-enable it and recreate the producer; duplicates written while it was off remain on the topic.",
    scope: "producer",
    goal: "Prevent acknowledged data loss",
    controls: "Ensures retried sends are not duplicated on the broker.",
    defaultValue: "true",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange: "Keep enabled unless a legacy broker version forces it off.",
    performanceImpact: "Negligible overhead; slightly constrains max.in.flight.requests.per.connection.",
    reliabilityImpact: "Eliminates duplicate records caused by producer retries.",
    relatedConfigs: ["acks", "retries", "max.in.flight.requests.per.connection"],
    failureModes: ["Duplicate records downstream when disabled and retries occur"],
  },
  {
    key: "retries",
    scope: "producer",
    goal: "Prevent acknowledged data loss",
    controls: "Number of times the producer automatically retries a send that fails with a retriable error.",
    defaultValue: "2147483647 (effectively unbounded)",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Rarely — delivery.timeout.ms is the practical bound on total retry time; lower this only to fail fast instead of retrying indefinitely within that window.",
    performanceImpact: "No direct throughput effect; more attempts are made before delivery.timeout.ms is reached.",
    reliabilityImpact:
      "Paired with enable.idempotence=true, retries make at-least-once delivery safe from transient errors without producing duplicates.",
    relatedConfigs: ["acks", "enable.idempotence", "delivery.timeout.ms", "max.in.flight.requests.per.connection"],
    failureModes: [
      "Retries without idempotence can reorder or duplicate records",
      "retries=0 turns any transient broker blip into a permanent send failure",
    ],
  },
  {
    key: "min.insync.replicas",
    exampleValue: "2",
    safeBaseline: "2 on a replication-factor-3 topic.",
    verification: "kafka-configs.sh --describe --topic <t> shows the override; stopping one of three brokers still allows acks=all writes, stopping two returns NOT_ENOUGH_REPLICAS.",
    rollback: "kafka-configs.sh --alter --delete-config min.insync.replicas (or set it back to 1) — effective immediately, no restart; rejected writes start succeeding again at once.",
    managedCaveat: "Some providers pin this and reject an override below their floor.",
    scope: "topic",
    goal: "Prevent acknowledged data loss",
    controls: "Minimum in-sync replicas required for a write with acks=all to succeed.",
    defaultValue: "1",
    changeMechanism: "topic-alter",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange: "Raise to 2 on a replication-factor-3 topic to survive one broker loss without data loss.",
    performanceImpact: "Higher values increase write latency under partial outages.",
    reliabilityImpact: "Directly bounds the durability guarantee acks=all can actually provide.",
    relatedConfigs: ["acks", "default.replication.factor", "unclean.leader.election.enable"],
    failureModes: ["NOT_ENOUGH_REPLICAS when ISR shrinks below this value"],
  },
  {
    key: "linger.ms",
    exampleValue: "20",
    safeBaseline: "5 (the Kafka 4.0 default); 10-100 on a high-throughput topic where a little added latency is acceptable.",
    verification: "Producer batch-size-avg, records-per-request, and compression-rate-avg rise while request rate to the broker falls.",
    rollback: "Set it back and recreate the producer — the change is immediate on the new client.",
    scope: "producer",
    goal: "Improve batching",
    controls: "How long the producer waits to fill a batch before sending it.",
    defaultValue: "0",
    defaultValueByVersion: { "4.0": "5" },
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Raise on high-throughput topics to trade a little latency for much better batching — Kafka 4.0 already defaults to 5ms; earlier versions default to 0.",
    performanceImpact: "Higher values improve compression ratio and broker-side throughput.",
    reliabilityImpact: "No direct effect on durability.",
    relatedConfigs: ["batch.size", "buffer.memory"],
    failureModes: ["Unnecessary tail latency if set too high for a latency-sensitive workload"],
  },
  {
    key: "batch.size",
    exampleValue: "65536",
    safeBaseline: "16384 (the default); raise it alongside linger.ms for large-record, high-throughput topics.",
    verification: "Producer batch-size-avg approaches the new value under load and buffer-available-bytes stays healthy.",
    rollback: "Restore 16384 and recreate the producer.",
    scope: "producer",
    goal: "Improve batching",
    controls: "Maximum size in bytes of a single batch per partition.",
    defaultValue: "16384",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange: "Raise alongside linger.ms for high-throughput, larger-record workloads.",
    performanceImpact: "Larger batches improve throughput and compression; too large wastes buffer memory.",
    reliabilityImpact: "No direct effect on durability.",
    relatedConfigs: ["linger.ms", "buffer.memory", "max.request.size"],
    failureModes: ["Increased memory pressure if oversized for the workload"],
  },
  {
    key: "buffer.memory",
    scope: "producer",
    goal: "Control memory and backpressure",
    controls: "Total bytes the producer can use to buffer records awaiting send, across all partitions.",
    defaultValue: "33554432",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Raise for high-throughput producers with many partitions or slow brokers that need more room to absorb bursts; lower to bound producer memory footprint.",
    performanceImpact:
      "Too small a buffer causes send() to block (and eventually time out via max.block.ms) whenever production briefly outpaces broker acknowledgment.",
    reliabilityImpact: "No direct durability effect — this is a backpressure control, not a durability one.",
    relatedConfigs: ["max.block.ms", "batch.size", "linger.ms"],
    failureModes: [
      "Producer threads block in send() when the buffer fills and the broker can't keep up",
      "TimeoutException once max.block.ms is exceeded while blocked",
    ],
  },
  {
    key: "max.block.ms",
    scope: "producer",
    goal: "Control memory and backpressure",
    controls: "How long send() and partitionsFor() may block when the buffer is full or metadata is unavailable, before throwing.",
    defaultValue: "60000",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Lower to fail fast in latency-sensitive applications instead of blocking the calling thread; raise if brief broker slowness is expected and acceptable to absorb.",
    performanceImpact: "Directly caps how long a calling thread can stall under backpressure.",
    reliabilityImpact: "Determines whether backpressure surfaces as an exception the application can react to, or as a long silent stall.",
    relatedConfigs: ["buffer.memory", "delivery.timeout.ms"],
    failureModes: ["Application threads appear hung for up to max.block.ms during a broker outage if not handled"],
  },
  {
    key: "max.request.size",
    scope: "producer",
    goal: "Handle large records",
    controls: "The largest single produce request the producer will send, which caps the largest record it will accept.",
    defaultValue: "1048576",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Raise only alongside the broker's matching message-size limit — raising one side without the other just moves where the rejection happens.",
    performanceImpact: "Larger allowed records increase per-request memory and network use and can increase tail latency for large payloads.",
    reliabilityImpact: "No durability effect; this is a validation limit enforced before a record is ever batched or sent.",
    relatedConfigs: ["batch.size", "buffer.memory"],
    failureModes: ["RecordTooLargeException thrown synchronously from send() for any record exceeding this limit, before it reaches the network"],
  },
  {
    key: "request.timeout.ms",
    scope: "producer",
    goal: "Bound request latency",
    controls: "How long the producer waits for a broker response to a single produce request before treating it as failed and retrying.",
    defaultValue: "30000",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Raise if brokers routinely take longer than 30s to ack under load (address the root cause first); lower to detect a stuck broker faster.",
    performanceImpact: "Too low a value causes retries against a broker that was only slightly slow, wasting the retry budget.",
    reliabilityImpact: "Must stay below delivery.timeout.ms so at least one full request/retry cycle can complete within the delivery budget.",
    relatedConfigs: ["delivery.timeout.ms", "retries"],
    failureModes: ["Spurious retries against a broker under transient load when set too low"],
  },
  {
    key: "delivery.timeout.ms",
    scope: "producer",
    goal: "Bound request latency",
    controls: "The total time budget from send() to final success or failure, covering batching, retries, and all request round trips.",
    defaultValue: "120000",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Raise for workloads that must tolerate longer broker unavailability without failing; lower to surface failures to the application faster.",
    performanceImpact: "No effect on the happy path; only changes how long the producer keeps retrying before giving up on a struggling record.",
    reliabilityImpact:
      "This — not retries alone — is the real bound on how long the producer keeps trying before surfacing a failure to the application.",
    relatedConfigs: ["retries", "request.timeout.ms", "linger.ms"],
    failureModes: ["TimeoutException delivered to the application's callback once this budget is exhausted, even with retries remaining"],
  },
  {
    key: "max.in.flight.requests.per.connection",
    scope: "producer",
    goal: "Preserve ordering during retries",
    controls: "How many produce requests the producer will send to a broker before receiving a response, per connection.",
    defaultValue: "5",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Leave at the default with enable.idempotence=true, which keeps ordering safe up to 5 in-flight requests via broker-side sequence checks; lower to 1 only if idempotence must stay disabled and strict ordering is required.",
    performanceImpact: "Higher values improve pipelining and throughput; values above 5 are rejected outright when idempotence is enabled.",
    reliabilityImpact:
      "Without idempotence, values above 1 allow a retried batch to be written out of order relative to a later batch that succeeded first.",
    relatedConfigs: ["enable.idempotence", "retries", "acks"],
    failureModes: ["Silent record reordering on retry when this is >1 and idempotence is disabled"],
  },
  {
    key: "key.serializer",
    exampleValue: "org.apache.kafka.common.serialization.StringSerializer",
    safeBaseline: "StringSerializer for string keys, ByteArraySerializer for pre-encoded keys, a schema-registry serializer when keys are registered.",
    verification: "A consumer with the matching key deserializer reads keys back as the original type; a mismatch throws SerializationException from poll().",
    rollback: "Revert the class and recreate the producer. Records already written keep the old encoding — a rollback does not rewrite them.",
    managedCaveat: "Purely client-side. With the provider's Schema Registry, use its schema-aware serializer and endpoint.",
    scope: "producer",
    goal: "Serialize records",
    controls:
      "The class that turns a record key object into the bytes stored on the topic. Must implement org.apache.kafka.common.serialization.Serializer.",
    defaultValue: "unset (required)",
    changeMechanism: "recreate-client",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange:
      "Pick it to match the key type you produce: StringSerializer for string keys, ByteArraySerializer for raw bytes, or a schema-aware serializer (Avro/Protobuf/JSON Schema) when the key is registered in a schema registry.",
    performanceImpact:
      "Schema-aware serializers add a registry lookup on the first record per schema (then cached) and a few bytes of wire header; primitive serializers are effectively free.",
    reliabilityImpact:
      "Consumers must deserialize keys with the matching type. Changing this on a live topic splits it into a before/after that no single consumer deserializer reads cleanly.",
    relatedConfigs: ["value.serializer", "partitioner.class"],
    failureModes: [
      "Key serializer and the consumer's key deserializer disagree — the consumer throws SerializationException out of poll() and the partition stalls",
      "Producing a key object the configured serializer cannot handle — send() throws synchronously",
    ],
  },
  {
    key: "value.serializer",
    exampleValue: "org.apache.kafka.common.serialization.StringSerializer",
    safeBaseline: "A schema-registry serializer when you want compatibility enforced at produce time; StringSerializer for JSON-as-text.",
    verification: "Consumers decode values with no errors; with a registry, the subject gains a new schema version only on a compatible change.",
    rollback: "Revert the class and recreate the producer; existing records keep their encoding.",
    managedCaveat: "Uses the provider's Schema Registry endpoint and credentials when schema-aware.",
    scope: "producer",
    goal: "Serialize records",
    controls:
      "The class that turns a record value object into the bytes stored on the topic. Must implement org.apache.kafka.common.serialization.Serializer.",
    defaultValue: "unset (required)",
    changeMechanism: "recreate-client",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange:
      "Match it to the value type and your data-contract strategy: StringSerializer for JSON-as-text, ByteArraySerializer for pre-encoded bytes, or a schema-registry serializer when you want the registry to enforce compatibility.",
    performanceImpact:
      "The dominant serialization cost for most workloads, since values are larger than keys; schema-aware serializers cache the schema after the first record.",
    reliabilityImpact:
      "The value encoding is the topic's data contract. A schema-registry serializer rejects an incompatible change at produce time; a plain serializer lets it through and breaks consumers instead.",
    relatedConfigs: ["key.serializer", "compression.type"],
    failureModes: [
      "Value serializer does not match the consumer's deserializer — every record fails to decode",
      "Switching value encodings without a registry gate, so old consumers hit undecodable records with no warning",
    ],
  },
  {
    key: "compression.type",
    exampleValue: "lz4",
    safeBaseline: "lz4 or zstd on any real workload; none only for the smallest lab.",
    verification: "The producer's request-size metric falls, broker bytes-in and topic disk usage drop, and consumers need no change.",
    rollback: "Set it back to none or the prior codec and recreate the producer. Already-written batches stay in their original compression and remain readable.",
    managedCaveat: "Fully client-controlled; where the provider bills on stored bytes, compression lowers that bill directly.",
    scope: "producer",
    goal: "Reduce bandwidth and storage cost",
    controls:
      "Whether the producer compresses each record batch before sending, and with which codec: none, gzip, snappy, lz4, or zstd. Compression is applied per batch, so it pairs with linger.ms and batch.size.",
    defaultValue: "none",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Turn it on (lz4 or zstd) for almost any real workload — it cuts network and disk use and usually raises throughput. zstd gives the best ratio; lz4 is the cheapest CPU. Larger linger.ms / batch.size make it more effective.",
    performanceImpact:
      "Producer CPU rises; network and broker disk use fall, and broker throughput usually improves because it stores and serves the batch already compressed. Consumers decompress transparently regardless of this setting.",
    reliabilityImpact:
      "No durability effect. If the topic's own compression.type is set to something other than 'producer', the broker recompresses on write, spending broker CPU.",
    relatedConfigs: ["linger.ms", "batch.size"],
    failureModes: [
      "gzip chosen for its ratio on a high-throughput producer, making producer CPU the bottleneck — lz4 or zstd are far cheaper for a similar result",
    ],
  },
  {
    key: "partitioner.class",
    safeBaseline: "Unset (null) — the built-in logic is what almost every workload wants.",
    verification: "Records with the same key land on the same partition (kafka-console-consumer --property print.partition=true, or an admin describe); no-key records spread over partitions across batches.",
    rollback: "Unset it and recreate the producer. Keys already written under the custom partitioner stay where they landed, so same-key ordering can be split until those records age out.",
    scope: "producer",
    goal: "Control partitioning",
    controls:
      "The class that decides which partition a record goes to. Unset (the default) uses Kafka's built-in logic; a custom class implements org.apache.kafka.clients.producer.Partitioner.",
    defaultValue: "null (built-in partitioning)",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Leave it unset unless you have a concrete routing requirement the default cannot meet (e.g. co-locating records from different topics on matching partitions). Kafka 3.3 deprecated the named DefaultPartitioner / UniformStickyPartitioner classes in favour of the unset default.",
    performanceImpact:
      "With the default: an explicit partition on the record wins; otherwise a key is hashed (murmur2) to a partition; a record with no key is placed in a partition that only switches once the current batch fills or linger.ms elapses, which keeps batches full.",
    reliabilityImpact:
      "Same-key ordering depends on the partitioner being stable. Changing it (or the partition count) re-routes existing keys, so records for one key can be split across two partitions during the transition and lose their relative order.",
    relatedConfigs: ["key.serializer", "linger.ms", "batch.size"],
    failureModes: [
      "A custom partitioner that ignores the key breaks the same-key-same-partition guarantee downstream consumers rely on",
      "Assuming no-key records round-robin per record — they are assigned batch-wise, so a low-volume producer can appear to use only one partition",
    ],
  },
  {
    key: "transactional.id",
    scope: "producer",
    goal: "Use transactions",
    controls: "A stable identifier that enables exactly-once semantics across sessions by fencing off any previous producer instance using the same ID.",
    defaultValue: "unset (transactions disabled)",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Set when a producer must write to multiple partitions/topics atomically, or participate in a consume-transform-produce exactly-once pipeline.",
    performanceImpact:
      "Adds coordinator round trips (initTransactions, commit) and requires acks=all and enable.idempotence=true, which this setting enables implicitly.",
    reliabilityImpact: "Enables atomic multi-partition writes and fences out zombie producer instances holding the same transactional.id.",
    relatedConfigs: ["enable.idempotence", "acks", "transaction.timeout.ms"],
    failureModes: [
      "ProducerFencedException when two producer instances share the same transactional.id concurrently",
      "Consumers configured for read_committed isolation see nothing until the transaction commits",
    ],
  },
  {
    key: "transaction.timeout.ms",
    scope: "producer",
    goal: "Use transactions",
    controls:
      "Maximum time the transaction coordinator waits, after the first partition is added to a transaction, for it to be committed or aborted before proactively aborting it.",
    defaultValue: "60000",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Raise only if the producer's own processing between adding the first partition and committing legitimately takes longer than a minute — and only up to the broker's transaction.max.timeout.ms ceiling.",
    performanceImpact: "No effect on the happy path; bounds how long a stalled transaction holds back read_committed consumers.",
    reliabilityImpact:
      "A transaction left open past this timeout is aborted automatically by the coordinator, preventing an indefinitely stuck consumer waiting on it. The clock starts when the first partition is added, not at beginTransaction().",
    relatedConfigs: ["transactional.id", "enable.idempotence"],
    failureModes: [
      "Setting this above the broker's transaction.max.timeout.ms fails producer initialization with InvalidTxnTimeoutException",
      "A transaction proactively aborted for exceeding this timeout fences the producer's next attempt to continue it",
    ],
  },
  {
    key: "max.poll.interval.ms",
    exampleValue: "600000",
    safeBaseline: "300000 (the default); raise it only if a batch legitimately needs longer, and lower max.poll.records first.",
    verification: "Rebalances stop firing mid-processing and the consumer stays in the group through its normal batch time.",
    rollback: "Restore 300000 and recreate the consumer.",
    scope: "consumer",
    goal: "Rebalance behavior",
    controls: "Maximum time allowed between polls before the consumer is considered dead and rebalanced out.",
    defaultValue: "300000",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Raise when per-record processing legitimately takes longer than 5 minutes.",
    performanceImpact: "Higher values delay detection of a genuinely stuck consumer.",
    reliabilityImpact: "Set too low, it causes rebalance storms under normal processing variance.",
    relatedConfigs: ["max.poll.records", "session.timeout.ms", "heartbeat.interval.ms"],
    failureModes: ["Rebalance loop when processing time exceeds this value", "Slow failure detection when set too high"],
  },
  {
    key: "session.timeout.ms",
    scope: "consumer",
    goal: "Rebalance behavior",
    controls: "How long the group coordinator waits without a heartbeat before considering a consumer dead (classic group protocol only).",
    defaultValue: "45000",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Lower for faster failure detection; raise for unstable networks with GC pauses. Ignored under group.protocol=consumer, where the broker controls session timing.",
    performanceImpact: "Lower values detect failure faster but risk false-positive rebalances.",
    reliabilityImpact: "Too aggressive a value causes unnecessary rebalances during transient pauses. Has no effect once the new consumer group protocol is in use.",
    relatedConfigs: ["heartbeat.interval.ms", "max.poll.interval.ms", "group.instance.id"],
    failureModes: ["Frequent rebalances from GC pauses or network blips shorter than expected"],
  },
  {
    key: "auto.offset.reset",
    exampleValue: "earliest",
    safeBaseline: "earliest for a consumer that must not skip records; latest for a live-only tail.",
    verification: "A brand-new group with earliest reads the full backlog; with latest it starts at the next produced record.",
    rollback: "Change it back and recreate the consumer. It only acts when there is no committed offset, so it does nothing for a group that has already committed.",
    scope: "consumer",
    goal: "Offset commits",
    controls: "What a consumer does when there is no committed offset for a partition.",
    defaultValue: "latest",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Use earliest for a new consumer group that must not skip existing data.",
    performanceImpact: "earliest can trigger a large initial backlog read.",
    reliabilityImpact: "latest silently skips any data produced before the group existed.",
    relatedConfigs: ["enable.auto.commit", "group.id"],
    failureModes: ["Silent data skip on latest for a newly created group"],
  },
  {
    key: "group.id",
    exampleValue: "order-fulfilment",
    safeBaseline: "One stable id per logical application, reused by every instance of it.",
    verification: "kafka-consumer-groups.sh --describe --group <id> lists exactly the instances you expect, sharing the partitions.",
    rollback: "Point back to the old id and restart. The new id has committed its own offsets, so the old group resumes from wherever it last committed — which may now be behind.",
    managedCaveat: "Provider ACLs are often granted per group.id prefix, so a new id can need an ACL change.",
    scope: "consumer",
    goal: "Consumer group scaling",
    controls: "The consumer group a consumer joins — the unit of partition assignment and offset tracking.",
    defaultValue: "unset (required for group consumption)",
    changeMechanism: "recreate-client",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange:
      "Pick one stable group.id per logical application. Change it only to deliberately start a fresh consumer position — a new group.id has no committed offsets and falls back to auto.offset.reset.",
    performanceImpact: "No direct performance effect; the number of instances sharing the group.id sets consumer parallelism, capped at the partition count.",
    reliabilityImpact:
      "Two applications that must each see all the data need different group.ids; instances that must share the work need the same one. Getting this backwards either duplicates or silently splits processing.",
    relatedConfigs: ["auto.offset.reset", "partition.assignment.strategy", "group.instance.id"],
    failureModes: [
      "Accidentally reusing one group.id across two applications makes them steal partitions from each other",
      "A new group.id silently starts at latest (or replays from earliest) instead of resuming",
    ],
  },
  {
    key: "partition.assignment.strategy",
    scope: "consumer",
    goal: "Rebalance behavior",
    controls: "Which assignor the group leader uses to map partitions to members, and whether rebalances are eager or cooperative (classic group protocol only).",
    defaultValue: "[RangeAssignor, CooperativeStickyAssignor]",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Set CooperativeStickyAssignor alone for large or latency-sensitive groups so a rebalance only pauses the partitions that actually move — the default list is headed by RangeAssignor, so an untouched group still does eager Range assignment. Ignored under group.protocol=consumer (assignor chosen server-side via group.remote.assignor).",
    performanceImpact: "Cooperative rebalancing keeps unaffected partitions flowing during a rebalance; eager assignors stop the entire group. The default list's first common assignor wins, and that is RangeAssignor.",
    reliabilityImpact: "All members of a group must share at least one common assignor. Moving from the default list to CooperativeStickyAssignor alone is one rolling bounce; migrating from an eager-only assignor that isn't already paired with a cooperative one takes two (add the cooperative one, then remove the eager one).",
    relatedConfigs: ["group.id", "group.instance.id", "max.poll.interval.ms"],
    failureModes: [
      "Removing the eager assignor before every instance has the cooperative one leaves the group unable to agree on a strategy",
      "Mixing eager and cooperative assignors in one group falls back to eager behavior",
    ],
  },
  {
    key: "group.instance.id",
    scope: "consumer",
    goal: "Rebalance behavior",
    controls: "A stable per-instance identity that makes a consumer a static group member, surviving restarts without a rebalance.",
    defaultValue: "unset (dynamic membership)",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Set a unique, stable value per instance (from the pod ordinal or host) to avoid a rebalance on every rolling restart, when brief slower failure detection is acceptable.",
    performanceImpact: "Eliminates the join/leave rebalance pair on a clean restart if the instance returns within session.timeout.ms.",
    reliabilityImpact:
      "A genuine failure of a static member isn't noticed until session.timeout.ms elapses, so this usually comes with a longer session timeout and fast-cycling deployment tooling.",
    relatedConfigs: ["session.timeout.ms", "group.id", "partition.assignment.strategy"],
    failureModes: [
      "Two live instances sharing one group.instance.id triggers a fencing error and one is kicked out",
      "A crashed static member's partitions stay unassigned until session.timeout.ms expires",
    ],
  },
  {
    key: "heartbeat.interval.ms",
    scope: "consumer",
    goal: "Rebalance behavior",
    controls: "How often the consumer's background thread sends a heartbeat to the group coordinator (classic group protocol only).",
    defaultValue: "3000",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange: "Rarely — keep it at roughly a third of session.timeout.ms so a few heartbeats can be missed before the session is declared dead. Ignored under group.protocol=consumer, where heartbeat cadence is broker-driven.",
    performanceImpact: "Lower values add negligible traffic but let the group notice a departed member and start rebalancing sooner.",
    reliabilityImpact: "Only detects a dead process or network partition; it does not detect a consumer stuck inside record processing — that is max.poll.interval.ms's job.",
    relatedConfigs: ["session.timeout.ms", "max.poll.interval.ms"],
    failureModes: ["Set too close to session.timeout.ms, a single missed heartbeat evicts a healthy consumer"],
  },
  {
    key: "max.poll.records",
    exampleValue: "100",
    safeBaseline: "500 (the default); lower it when per-record processing is slow.",
    verification: "poll() returns at most this many records and the loop's time away from poll() stays well under max.poll.interval.ms.",
    rollback: "Restore 500 and recreate the consumer.",
    scope: "consumer",
    goal: "Rebalance behavior",
    controls: "The maximum number of records a single poll() call returns to the application.",
    defaultValue: "500",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Lower it when per-record processing is slow, so one batch's total processing time stays well under max.poll.interval.ms; raise it for very fast handlers to cut per-poll overhead.",
    performanceImpact: "Larger batches amortize poll overhead; smaller batches bound how long the loop is away from poll() and keep heartbeating implicit in the loop honest.",
    reliabilityImpact: "This is the primary lever for preventing the consumer from being rebalanced out for exceeding max.poll.interval.ms.",
    relatedConfigs: ["max.poll.interval.ms", "fetch.max.bytes"],
    failureModes: ["A large batch of slow-to-process records blows past max.poll.interval.ms and forces the consumer out of the group"],
  },
  {
    key: "fetch.max.bytes",
    scope: "consumer",
    goal: "Tune consumer fetching",
    controls: "The maximum amount of data the broker returns for a single fetch request, across all fetched partitions.",
    defaultValue: "52428800 (50 MiB)",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Lower it to cap consumer heap use when records are large; raise it for high-throughput consumers on a fast network. It is a soft limit — if the first record batch in the first non-empty partition is larger than this, the batch is still returned so the consumer can make progress.",
    performanceImpact: "Larger fetches improve throughput but hold more data in the client between polls; max.poll.records then paces how much of it each poll() hands to the application.",
    reliabilityImpact: "Does not affect durability; an undersized value on a busy topic just means more fetch round trips.",
    relatedConfigs: ["max.poll.records"],
    failureModes: ["Set very low with large record batches, the consumer spends most of its time on fetch overhead"],
  },
  {
    key: "key.deserializer",
    exampleValue: "org.apache.kafka.common.serialization.StringDeserializer",
    safeBaseline: "The counterpart of the producer's key.serializer.",
    verification: "poll() returns records whose keys are the expected type, with no SerializationException.",
    rollback: "Revert the class and recreate the consumer.",
    scope: "consumer",
    goal: "Serialize records",
    controls:
      "The class that turns the stored key bytes back into an object. Must implement org.apache.kafka.common.serialization.Deserializer and match the type the producer serialized.",
    defaultValue: "unset (required)",
    changeMechanism: "recreate-client",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange:
      "Set it to the counterpart of the producer's key.serializer: StringDeserializer for string keys, ByteArrayDeserializer for raw bytes, the schema-aware deserializer when keys are registered.",
    performanceImpact:
      "Negligible for primitives; a schema-aware deserializer does a cached registry lookup per schema id it has not seen.",
    reliabilityImpact:
      "A mismatch with the produced bytes throws SerializationException from inside poll(), before the record reaches your code — the partition stops advancing until you fix the deserializer or skip the offset.",
    relatedConfigs: ["value.deserializer", "key.serializer"],
    failureModes: [
      "Consumer configured for StringDeserializer against keys written as Avro — poll() throws and the group makes no progress on that partition",
    ],
  },
  {
    key: "value.deserializer",
    exampleValue: "org.apache.kafka.common.serialization.StringDeserializer",
    safeBaseline: "The counterpart of the producer's value.serializer; wrap it in an error-handling deserializer if poison records are a concern.",
    verification: "poll() returns decoded values; a record it cannot parse throws from inside poll() and the partition stops advancing.",
    rollback: "Revert the class and recreate the consumer.",
    scope: "consumer",
    goal: "Serialize records",
    controls:
      "The class that turns the stored value bytes back into an object. Must implement org.apache.kafka.common.serialization.Deserializer and match the producer's value.serializer.",
    defaultValue: "unset (required)",
    changeMechanism: "recreate-client",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange:
      "Match it to the producer's value encoding. With a schema registry, the deserializer looks up each record's schema by the id in the wire header, so one consumer can read values written against several schema versions.",
    performanceImpact:
      "The main per-record CPU cost on the consumer; schema-aware deserializers cache each schema after first use.",
    reliabilityImpact:
      "A poison record (bytes this deserializer cannot parse) throws out of poll() and stalls the partition exactly like a business-logic failure would — handling it needs either a try/catch deserializer wrapper or an error-handling policy.",
    relatedConfigs: ["key.deserializer", "value.serializer", "isolation.level"],
    failureModes: [
      "One malformed record on a partition stops all consumption of that partition until the offset is skipped",
      "Deserializer type does not match what was produced — nothing on the topic decodes",
    ],
  },
  {
    key: "fetch.min.bytes",
    exampleValue: "65536",
    safeBaseline: "Leave at 1 unless a high-rate topic is generating an excessive fetch-request volume.",
    verification: "Broker fetch-request rate drops and average fetch size rises; poll latency on a quiet topic rises by up to fetch.max.wait.ms.",
    rollback: "Restore 1 and recreate the consumer.",
    scope: "consumer",
    goal: "Tune consumer fetching",
    controls:
      "The minimum amount of data a broker waits to accumulate before answering a fetch request. The broker responds sooner if fetch.max.wait.ms elapses first.",
    defaultValue: "1",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Raise it (to a few KB or more) on a high-message-rate topic to get fewer, fuller fetch responses — less CPU and network overhead on both sides — when an extra few hundred milliseconds of latency is acceptable.",
    performanceImpact:
      "Higher values improve throughput efficiency and reduce request rate; they add up to fetch.max.wait.ms of latency when the topic is quiet.",
    reliabilityImpact: "No durability or delivery effect — purely a batching-versus-latency trade on the read path.",
    relatedConfigs: ["fetch.max.wait.ms", "fetch.max.bytes", "max.partition.fetch.bytes"],
    failureModes: [
      "Raised high on a low-traffic topic without adjusting fetch.max.wait.ms — every poll waits the full 500 ms for data that never reaches the threshold",
    ],
  },
  {
    key: "fetch.max.wait.ms",
    safeBaseline: "Leave at 500; lower it only when fetch.min.bytes is raised and idle-topic latency matters.",
    rollback: "Restore 500 and recreate the consumer.",
    scope: "consumer",
    goal: "Tune consumer fetching",
    controls: "The longest a broker holds a fetch request open waiting for fetch.min.bytes of data before responding with whatever it has.",
    defaultValue: "500",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Lower it when fetch.min.bytes is raised and the added tail latency on a quiet topic matters; leave it at the default when fetch.min.bytes is 1.",
    performanceImpact: "Only takes effect when fetch.min.bytes is not yet met — it bounds the wait, so lower values cut worst-case poll latency on idle partitions.",
    reliabilityImpact: "None.",
    relatedConfigs: ["fetch.min.bytes", "fetch.max.bytes"],
    failureModes: ["Confused with a poll timeout — this only caps the broker-side wait for a single fetch, not how long poll() blocks overall"],
  },
  {
    key: "max.partition.fetch.bytes",
    exampleValue: "5242880",
    safeBaseline: "Leave at 1 MiB; raise it for large records, lower it to bound memory when subscribed to many partitions.",
    verification: "Fewer fetch round trips per partition to drain a backlog; consumer heap between polls scales with this value times the assigned partition count.",
    rollback: "Restore 1048576 and recreate the consumer.",
    scope: "consumer",
    goal: "Tune consumer fetching",
    controls: "The maximum data the broker returns for any one partition in a fetch response. fetch.max.bytes caps the whole response across partitions.",
    defaultValue: "1048576",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Raise it when records are large and one partition should be able to return more per round trip; lower it to bound per-partition memory when a consumer subscribes to many partitions at once.",
    performanceImpact:
      "Larger values mean fewer, fuller batches per partition and more client memory held between polls. It has a soft-limit escape: if the first record batch in the first non-empty partition of a fetch is larger than this, the broker returns that one batch anyway so the consumer can always make progress.",
    reliabilityImpact:
      "The soft-limit escape means an oversized record batch never wedges a consumer — it is guaranteed to come back whole in the first non-empty partition of a fetch, even though it exceeds the cap.",
    relatedConfigs: ["fetch.max.bytes", "max.poll.records"],
    failureModes: [
      "Set well below the topic's record-batch size, so each partition returns fewer batches per fetch and the consumer needs more fetch round trips to drain a backlog",
    ],
  },
  {
    key: "client.rack",
    exampleValue: "us-east-1a",
    safeBaseline: "The consumer's zone in a multi-AZ deployment; empty otherwise.",
    verification: "The consumer's cross-AZ data-transfer cost drops and broker request metrics show the in-zone follower serving its fetches.",
    rollback: "Clear it and recreate the consumer — fetches revert to the partition leader immediately.",
    managedCaveat: "Only takes effect if the provider runs a rack-aware replica selector and exposes zone ids; several do for this exact cost saving, some do not.",
    scope: "consumer",
    goal: "Tune consumer fetching",
    controls:
      "The rack or availability-zone id of this consumer. When brokers are configured with broker.rack and a rack-aware replica selector, the consumer fetches from the in-zone replica (often a follower) instead of always the partition leader.",
    defaultValue: '""',
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "limited",
    whenToChange:
      "Set it to the consumer's zone in a multi-AZ deployment to cut cross-zone network transfer cost — but only if the brokers run replica.selector.class=RackAwareReplicaSelector; without that it is silently ignored.",
    performanceImpact:
      "Reduces cross-zone bandwidth and can lower fetch latency by reading a closer replica. A follower only serves up to the high watermark, so a consumer may sit a few milliseconds further behind the leader.",
    reliabilityImpact:
      "No correctness impact — followers never return uncommitted data. If the in-zone replica falls out of the ISR the consumer transparently falls back to the leader.",
    relatedConfigs: ["fetch.min.bytes"],
    failureModes: [
      "Set on the consumer but the brokers have no rack-aware selector configured — nothing changes and the expected cost saving never appears",
    ],
  },
  {
    key: "allow.auto.create.topics",
    safeBaseline: "false in production, paired with startup topic-existence validation; true is only a lab convenience.",
    verification: "Subscribing to a missing topic no longer creates it — the consumer logs UNKNOWN_TOPIC_OR_PARTITION and takes no assignment.",
    rollback: "Set it back to true and recreate the consumer; any junk topics already created must be deleted separately.",
    managedCaveat: "Most providers disable broker-side auto-create, so this setting has no effect there regardless.",
    scope: "consumer",
    goal: "Control topic auto-creation",
    controls:
      "Whether this consumer may trigger creation of a topic it subscribes to but that does not exist yet. It only has an effect when the broker also has auto.create.topics.enable set.",
    defaultValue: "true",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "limited",
    whenToChange:
      "Set it to false in production so a typo in a topic name does not silently create a 1-partition, default-replication topic that then accumulates data no one reads correctly. It does not make the typo fail loudly on its own — the broker returns UNKNOWN_TOPIC_OR_PARTITION and the consumer just keeps polling and refreshing metadata — so pair it with startup topic-existence validation or a lag/assignment alert.",
    performanceImpact: "None.",
    reliabilityImpact:
      "An accidentally auto-created topic is created with broker defaults (often replication factor 1), so data written to it before anyone notices has no fault tolerance. Many managed services disable broker-side auto-create, making this setting moot.",
    relatedConfigs: ["auto.offset.reset", "group.id"],
    failureModes: [
      "With it left true, a misspelled topic name auto-creates an empty topic; with it false, the same typo leaves the consumer polling a topic that does not exist — either way it looks like an idle consumer unless monitoring catches it",
    ],
  },
  {
    key: "default.api.timeout.ms",
    exampleValue: "30000",
    safeBaseline: "Leave at 60000, and keep it at least as large as request.timeout.ms.",
    verification: "A blocking call such as commitSync() against a slow coordinator returns or throws within the new budget instead of the old one.",
    rollback: "Restore 60000 and recreate the consumer.",
    scope: "consumer",
    goal: "Bound request latency",
    controls:
      "The overall deadline for blocking consumer calls that do not take an explicit timeout argument — commitSync(), partitionsFor(), listTopics(), position(), committed(). request.timeout.ms bounds each individual request the call makes inside that deadline.",
    defaultValue: "60000",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Lower it to shrink the total time an application will block on one of these calls before it gives up and can react; raise it only if legitimate operations against a busy cluster routinely need more than a minute.",
    performanceImpact: "No happy-path effect — a call that completes quickly still returns quickly. It only sets how long a call keeps retrying failed requests before giving up.",
    reliabilityImpact:
      "This is the retry/time budget for the whole operation. Setting it below request.timeout.ms is allowed but means a single slow request can consume the entire budget, leaving no room for a retry.",
    relatedConfigs: ["request.timeout.ms", "max.poll.interval.ms"],
    failureModes: [
      "Set very low, a routine commitSync() against a briefly slow coordinator throws TimeoutException before its retries can land",
    ],
  },
  {
    key: "group.protocol",
    scope: "consumer",
    goal: "Rebalance behavior",
    controls: "Which consumer group protocol the client uses: classic (client-side assignment) or consumer (broker-side assignment). The consumer protocol was early access in Kafka 3.7–3.9 and became production-ready in 4.0.",
    defaultValue: "classic",
    // KIP-848: early access from 3.7, production-ready in 4.0. 3.7 predates every selectable
    // version, so the entry shows on all of them — flagged early access below 4.0.
    availableFromVersion: "3.7",
    earlyAccessUntilVersion: "4.0",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "limited",
    whenToChange:
      "Move to consumer to get incremental, broker-coordinated rebalances without tuning client-side assignors or heartbeat timers. Only rely on it in production from 4.0 onward — in 3.7–3.9 it is early access, off by default on the broker, and missing pieces like regex subscriptions.",
    performanceImpact:
      "The consumer protocol removes the global synchronization barrier from a rebalance: members learn about assignment changes individually through their heartbeat responses instead of all rejoining at once.",
    reliabilityImpact:
      "Under group.protocol=consumer the client-side knobs session.timeout.ms, heartbeat.interval.ms and partition.assignment.strategy are ignored — session and heartbeat timing become broker-controlled, and the assignor is selected server-side (group.remote.assignor).",
    relatedConfigs: ["partition.assignment.strategy", "session.timeout.ms", "heartbeat.interval.ms"],
    failureModes: [
      "Setting group.protocol=consumer against brokers that don't support or haven't enabled the new protocol fails to join the group",
      "Client-side assignor and heartbeat tuning silently has no effect once the consumer protocol is in use",
    ],
  },
  {
    key: "enable.auto.commit",
    safeBaseline: "false for any consumer whose processing must not be lost — commit after the work, not on a timer.",
    verification: "Crash the consumer mid-batch: with false the uncommitted records are redelivered on restart; with true some can be silently skipped.",
    rollback: "Set it back to true and recreate the consumer, accepting the weaker delivery guarantee.",
    scope: "consumer",
    goal: "Offset commits",
    controls: "Whether the consumer commits offsets automatically during poll(), or leaves committing to the application.",
    defaultValue: "true",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Disable it whenever processing must be tied to the commit — write results, then commitSync — so a crash re-delivers only work that wasn't finished rather than silently skipping it.",
    performanceImpact: "Auto-commit is slightly cheaper (piggybacked on poll); frequent synchronous manual commits add a round trip each.",
    reliabilityImpact:
      "Auto-commit advances the offset on poll() timing, not on work completion — a crash after poll() but before processing finishes loses those records. It is at-least-once only if you always finish a batch before the next poll().",
    relatedConfigs: ["auto.commit.interval.ms", "auto.offset.reset", "isolation.level"],
    failureModes: [
      "Silent data loss when work outlives the poll loop and a crash follows an auto-commit",
      "Duplicate processing on any crash, since the last interval's worth of processed records was not yet committed",
    ],
  },
  {
    key: "auto.commit.interval.ms",
    scope: "consumer",
    goal: "Offset commits",
    controls: "The minimum time between automatic offset commits when enable.auto.commit is true.",
    defaultValue: "5000",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange: "Lower it to shrink the window of records that are reprocessed after a crash; it has no effect when enable.auto.commit is false.",
    performanceImpact: "More frequent commits add coordinator traffic; the default 5s is negligible for most groups.",
    reliabilityImpact: "Bounds how much already-processed work is replayed on an unclean restart, but never eliminates it — only manual commit-after-processing does that.",
    relatedConfigs: ["enable.auto.commit"],
    failureModes: ["A large interval means a crash reprocesses up to that many seconds of records"],
  },
  {
    key: "isolation.level",
    scope: "consumer",
    goal: "Read transactional data",
    controls: "Whether the consumer sees records from open and aborted transactions, or only committed ones.",
    defaultValue: "read_uncommitted",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Set read_committed on any consumer downstream of a transactional producer, so it never processes records that later get aborted.",
    performanceImpact: "read_committed can only advance to the last stable offset, so a long-running open transaction upstream stalls the consumer even though newer committed data exists behind it.",
    reliabilityImpact: "read_uncommitted will deliver records that belong to a transaction that is later aborted, which downstream systems then have to compensate for.",
    relatedConfigs: ["enable.auto.commit"],
    failureModes: [
      "read_uncommitted consumers act on records from transactions that never commit",
      "A stuck upstream transaction blocks a read_committed consumer until transaction.timeout.ms aborts it",
    ],
  },
  {
    key: "default.replication.factor",
    exampleValue: "3",
    safeBaseline: "3 in any cluster you cannot afford to lose data from.",
    verification: "A newly auto-created topic shows replication factor 3 in kafka-topics.sh --describe.",
    rollback: "Set it back (broker restart). Existing topics keep the replication factor they were created with; only newly auto-created topics change.",
    managedCaveat: "The provider fixes the replication factor (usually 3) and does not expose this.",
    scope: "broker",
    goal: "Replication and durability",
    controls: "Replication factor applied to auto-created topics.",
    defaultValue: "1",
    changeMechanism: "broker-restart",
    riskOfChange: "high-risk",
    managedAvailability: "limited",
    whenToChange: "Set to 3 in any production cluster; 1 has no fault tolerance.",
    performanceImpact: "Higher replication increases network and disk I/O per write.",
    reliabilityImpact: "Directly determines how many broker failures a topic can survive.",
    relatedConfigs: ["min.insync.replicas", "unclean.leader.election.enable"],
    failureModes: ["Total partition data loss on single-broker failure at replication factor 1"],
  },
  {
    key: "unclean.leader.election.enable",
    scope: "broker",
    goal: "Replication and durability",
    controls: "Whether an out-of-sync replica may be elected leader to restore availability.",
    defaultValue: "false",
    changeMechanism: "dynamic-cluster",
    riskOfChange: "high-risk",
    managedAvailability: "limited",
    whenToChange: "Enable temporarily only to restore availability when no in-sync replica is electable, knowing this can lose data.",
    performanceImpact: "No direct performance effect.",
    reliabilityImpact: "Enabling trades durability for availability; can commit an out-of-sync log as truth.",
    relatedConfigs: ["min.insync.replicas", "default.replication.factor"],
    failureModes: ["Silent data loss and divergent history when enabled during an outage"],
  },
  {
    key: "num.partitions",
    scope: "broker",
    goal: "Segment management",
    controls: "Default partition count for auto-created topics.",
    defaultValue: "1",
    changeMechanism: "dynamic-cluster",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Size explicitly per topic based on target throughput and consumer parallelism, not the cluster default.",
    performanceImpact: "More partitions increase parallelism but add per-partition overhead on brokers and clients.",
    reliabilityImpact: "Partition count cannot be decreased later without recreating the topic.",
    relatedConfigs: ["default.replication.factor"],
    failureModes: ["Hot partitions from too few partitions relative to key cardinality"],
  },
  {
    key: "advertised.listeners",
    scope: "broker",
    goal: "Listener configuration",
    controls: "The address clients use to connect to a broker after the initial bootstrap.",
    defaultValue: "unset (must be configured)",
    changeMechanism: "broker-restart",
    riskOfChange: "high-risk",
    managedAvailability: "unavailable",
    whenToChange: "Configure explicitly whenever brokers are reachable via a different address than they bind to (containers, cloud NAT).",
    performanceImpact: "No direct performance effect.",
    reliabilityImpact: "Misconfiguration causes clients to connect successfully then fail on every subsequent request.",
    relatedConfigs: ["listeners", "listener.security.protocol.map"],
    failureModes: ["Producers/consumers can bootstrap but then time out reaching the real broker address"],
  },
];

export const configGoals = Array.from(new Set(configs.map((c) => c.goal)));
export const configScopes = Array.from(new Set(configs.map((c) => c.scope)));
