import { ConfigEntry } from "@/lib/types";

export const configs: ConfigEntry[] = [
  {
    key: "acks",
    scope: "producer",
    goal: "Prevent acknowledged data loss",
    controls: "How many replicas must confirm a write before the producer considers it successful.",
    defaultValue: "all (Kafka 3.0+ clients)",
    changeMechanism: "recreate-client",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange: "Lower only for throughput-over-durability workloads (metrics, logs you can afford to lose).",
    performanceImpact: "acks=0 and acks=1 reduce latency; acks=all adds a round trip to the ISR.",
    reliabilityImpact: "acks=all is required for no-data-loss guarantees when paired with min.insync.replicas.",
    relatedConfigs: ["min.insync.replicas", "enable.idempotence", "retries"],
    failureModes: ["Silent data loss on leader failure when acks=0 or acks=1", "NOT_ENOUGH_REPLICAS with acks=all and a thin ISR"],
  },
  {
    key: "enable.idempotence",
    scope: "producer",
    goal: "Prevent acknowledged data loss",
    controls: "Ensures retried sends are not duplicated on the broker.",
    defaultValue: "true",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange: "Keep enabled unless a legacy broker version forces it off.",
    performanceImpact: "Negligible overhead; slightly constrains max.in.flight.requests.per.connection.",
    reliabilityImpact: "Eliminates duplicate records caused by producer retries.",
    relatedConfigs: ["acks", "retries", "max.in.flight.requests.per.connection"],
    failureModes: ["Duplicate records downstream when disabled and retries occur"],
  },
  {
    key: "retries",
    scope: "producer",
    goal: "Prevent acknowledged data loss",
    controls: "Number of times the producer automatically retries a send that fails with a retriable error.",
    defaultValue: "2147483647 (effectively unbounded)",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Rarely — delivery.timeout.ms is the practical bound on total retry time; lower this only to fail fast instead of retrying indefinitely within that window.",
    performanceImpact: "No direct throughput effect; more attempts are made before delivery.timeout.ms is reached.",
    reliabilityImpact:
      "Paired with enable.idempotence=true, retries make at-least-once delivery safe from transient errors without producing duplicates.",
    relatedConfigs: ["acks", "enable.idempotence", "delivery.timeout.ms", "max.in.flight.requests.per.connection"],
    failureModes: [
      "Retries without idempotence can reorder or duplicate records",
      "retries=0 turns any transient broker blip into a permanent send failure",
    ],
  },
  {
    key: "min.insync.replicas",
    scope: "topic",
    goal: "Prevent acknowledged data loss",
    controls: "Minimum in-sync replicas required for a write with acks=all to succeed.",
    defaultValue: "1",
    changeMechanism: "topic-alter",
    riskOfChange: "high-risk",
    managedAvailability: "full",
    whenToChange: "Raise to 2 on a replication-factor-3 topic to survive one broker loss without data loss.",
    performanceImpact: "Higher values increase write latency under partial outages.",
    reliabilityImpact: "Directly bounds the durability guarantee acks=all can actually provide.",
    relatedConfigs: ["acks", "default.replication.factor", "unclean.leader.election.enable"],
    failureModes: ["NOT_ENOUGH_REPLICAS when ISR shrinks below this value"],
  },
  {
    key: "linger.ms",
    scope: "producer",
    goal: "Improve batching",
    controls: "How long the producer waits to fill a batch before sending it.",
    defaultValue: "0",
    defaultValueByVersion: { "4.0": "5" },
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Raise on high-throughput topics to trade a little latency for much better batching — Kafka 4.0 already defaults to 5ms; earlier versions default to 0.",
    performanceImpact: "Higher values improve compression ratio and broker-side throughput.",
    reliabilityImpact: "No direct effect on durability.",
    relatedConfigs: ["batch.size", "buffer.memory"],
    failureModes: ["Unnecessary tail latency if set too high for a latency-sensitive workload"],
  },
  {
    key: "batch.size",
    scope: "producer",
    goal: "Improve batching",
    controls: "Maximum size in bytes of a single batch per partition.",
    defaultValue: "16384",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange: "Raise alongside linger.ms for high-throughput, larger-record workloads.",
    performanceImpact: "Larger batches improve throughput and compression; too large wastes buffer memory.",
    reliabilityImpact: "No direct effect on durability.",
    relatedConfigs: ["linger.ms", "buffer.memory", "max.request.size"],
    failureModes: ["Increased memory pressure if oversized for the workload"],
  },
  {
    key: "buffer.memory",
    scope: "producer",
    goal: "Control memory and backpressure",
    controls: "Total bytes the producer can use to buffer records awaiting send, across all partitions.",
    defaultValue: "33554432",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Raise for high-throughput producers with many partitions or slow brokers that need more room to absorb bursts; lower to bound producer memory footprint.",
    performanceImpact:
      "Too small a buffer causes send() to block (and eventually time out via max.block.ms) whenever production briefly outpaces broker acknowledgment.",
    reliabilityImpact: "No direct durability effect — this is a backpressure control, not a durability one.",
    relatedConfigs: ["max.block.ms", "batch.size", "linger.ms"],
    failureModes: [
      "Producer threads block in send() when the buffer fills and the broker can't keep up",
      "TimeoutException once max.block.ms is exceeded while blocked",
    ],
  },
  {
    key: "max.block.ms",
    scope: "producer",
    goal: "Control memory and backpressure",
    controls: "How long send() and partitionsFor() may block when the buffer is full or metadata is unavailable, before throwing.",
    defaultValue: "60000",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Lower to fail fast in latency-sensitive applications instead of blocking the calling thread; raise if brief broker slowness is expected and acceptable to absorb.",
    performanceImpact: "Directly caps how long a calling thread can stall under backpressure.",
    reliabilityImpact: "Determines whether backpressure surfaces as an exception the application can react to, or as a long silent stall.",
    relatedConfigs: ["buffer.memory", "delivery.timeout.ms"],
    failureModes: ["Application threads appear hung for up to max.block.ms during a broker outage if not handled"],
  },
  {
    key: "max.request.size",
    scope: "producer",
    goal: "Handle large records",
    controls: "The largest single produce request the producer will send, which caps the largest record it will accept.",
    defaultValue: "1048576",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Raise only alongside the broker's matching message-size limit — raising one side without the other just moves where the rejection happens.",
    performanceImpact: "Larger allowed records increase per-request memory and network use and can increase tail latency for large payloads.",
    reliabilityImpact: "No durability effect; this is a validation limit enforced before a record is ever batched or sent.",
    relatedConfigs: ["batch.size", "buffer.memory"],
    failureModes: ["RecordTooLargeException thrown synchronously from send() for any record exceeding this limit, before it reaches the network"],
  },
  {
    key: "request.timeout.ms",
    scope: "producer",
    goal: "Bound request latency",
    controls: "How long the producer waits for a broker response to a single produce request before treating it as failed and retrying.",
    defaultValue: "30000",
    changeMechanism: "recreate-client",
    riskOfChange: "safe",
    managedAvailability: "full",
    whenToChange:
      "Raise if brokers routinely take longer than 30s to ack under load (address the root cause first); lower to detect a stuck broker faster.",
    performanceImpact: "Too low a value causes retries against a broker that was only slightly slow, wasting the retry budget.",
    reliabilityImpact: "Must stay below delivery.timeout.ms so at least one full request/retry cycle can complete within the delivery budget.",
    relatedConfigs: ["delivery.timeout.ms", "retries"],
    failureModes: ["Spurious retries against a broker under transient load when set too low"],
  },
  {
    key: "delivery.timeout.ms",
    scope: "producer",
    goal: "Bound request latency",
    controls: "The total time budget from send() to final success or failure, covering batching, retries, and all request round trips.",
    defaultValue: "120000",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Raise for workloads that must tolerate longer broker unavailability without failing; lower to surface failures to the application faster.",
    performanceImpact: "No effect on the happy path; only changes how long the producer keeps retrying before giving up on a struggling record.",
    reliabilityImpact:
      "This — not retries alone — is the real bound on how long the producer keeps trying before surfacing a failure to the application.",
    relatedConfigs: ["retries", "request.timeout.ms", "linger.ms"],
    failureModes: ["TimeoutException delivered to the application's callback once this budget is exhausted, even with retries remaining"],
  },
  {
    key: "max.in.flight.requests.per.connection",
    scope: "producer",
    goal: "Preserve ordering during retries",
    controls: "How many produce requests the producer will send to a broker before receiving a response, per connection.",
    defaultValue: "5",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Leave at the default with enable.idempotence=true, which keeps ordering safe up to 5 in-flight requests via broker-side sequence checks; lower to 1 only if idempotence must stay disabled and strict ordering is required.",
    performanceImpact: "Higher values improve pipelining and throughput; values above 5 are rejected outright when idempotence is enabled.",
    reliabilityImpact:
      "Without idempotence, values above 1 allow a retried batch to be written out of order relative to a later batch that succeeded first.",
    relatedConfigs: ["enable.idempotence", "retries", "acks"],
    failureModes: ["Silent record reordering on retry when this is >1 and idempotence is disabled"],
  },
  {
    key: "transactional.id",
    scope: "producer",
    goal: "Use transactions",
    controls: "A stable identifier that enables exactly-once semantics across sessions by fencing off any previous producer instance using the same ID.",
    defaultValue: "unset (transactions disabled)",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Set when a producer must write to multiple partitions/topics atomically, or participate in a consume-transform-produce exactly-once pipeline.",
    performanceImpact:
      "Adds coordinator round trips (initTransactions, commit) and requires acks=all and enable.idempotence=true, which this setting enables implicitly.",
    reliabilityImpact: "Enables atomic multi-partition writes and fences out zombie producer instances holding the same transactional.id.",
    relatedConfigs: ["enable.idempotence", "acks", "transaction.timeout.ms"],
    failureModes: [
      "ProducerFencedException when two producer instances share the same transactional.id concurrently",
      "Consumers configured for read_committed isolation see nothing until the transaction commits",
    ],
  },
  {
    key: "transaction.timeout.ms",
    scope: "producer",
    goal: "Use transactions",
    controls:
      "Maximum time the transaction coordinator waits, after the first partition is added to a transaction, for it to be committed or aborted before proactively aborting it.",
    defaultValue: "60000",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange:
      "Raise only if the producer's own processing between adding the first partition and committing legitimately takes longer than a minute — and only up to the broker's transaction.max.timeout.ms ceiling.",
    performanceImpact: "No effect on the happy path; bounds how long a stalled transaction holds back read_committed consumers.",
    reliabilityImpact:
      "A transaction left open past this timeout is aborted automatically by the coordinator, preventing an indefinitely stuck consumer waiting on it. The clock starts when the first partition is added, not at beginTransaction().",
    relatedConfigs: ["transactional.id", "enable.idempotence"],
    failureModes: [
      "Setting this above the broker's transaction.max.timeout.ms fails producer initialization with InvalidTxnTimeoutException",
      "A transaction proactively aborted for exceeding this timeout fences the producer's next attempt to continue it",
    ],
  },
  {
    key: "max.poll.interval.ms",
    scope: "consumer",
    goal: "Rebalance behavior",
    controls: "Maximum time allowed between polls before the consumer is considered dead and rebalanced out.",
    defaultValue: "300000",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Raise when per-record processing legitimately takes longer than 5 minutes.",
    performanceImpact: "Higher values delay detection of a genuinely stuck consumer.",
    reliabilityImpact: "Set too low, it causes rebalance storms under normal processing variance.",
    relatedConfigs: ["max.poll.records", "session.timeout.ms", "heartbeat.interval.ms"],
    failureModes: ["Rebalance loop when processing time exceeds this value", "Slow failure detection when set too high"],
  },
  {
    key: "session.timeout.ms",
    scope: "consumer",
    goal: "Rebalance behavior",
    controls: "How long the group coordinator waits without a heartbeat before considering a consumer dead.",
    defaultValue: "45000",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Lower for faster failure detection; raise for unstable networks with GC pauses.",
    performanceImpact: "Lower values detect failure faster but risk false-positive rebalances.",
    reliabilityImpact: "Too aggressive a value causes unnecessary rebalances during transient pauses.",
    relatedConfigs: ["heartbeat.interval.ms", "max.poll.interval.ms", "group.instance.id"],
    failureModes: ["Frequent rebalances from GC pauses or network blips shorter than expected"],
  },
  {
    key: "auto.offset.reset",
    scope: "consumer",
    goal: "Offset commits",
    controls: "What a consumer does when there is no committed offset for a partition.",
    defaultValue: "latest",
    changeMechanism: "recreate-client",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Use earliest for a new consumer group that must not skip existing data.",
    performanceImpact: "earliest can trigger a large initial backlog read.",
    reliabilityImpact: "latest silently skips any data produced before the group existed.",
    relatedConfigs: ["enable.auto.commit", "group.id"],
    failureModes: ["Silent data skip on latest for a newly created group"],
  },
  {
    key: "default.replication.factor",
    scope: "broker",
    goal: "Replication and durability",
    controls: "Replication factor applied to auto-created topics.",
    defaultValue: "1",
    changeMechanism: "broker-restart",
    riskOfChange: "high-risk",
    managedAvailability: "limited",
    whenToChange: "Set to 3 in any production cluster; 1 has no fault tolerance.",
    performanceImpact: "Higher replication increases network and disk I/O per write.",
    reliabilityImpact: "Directly determines how many broker failures a topic can survive.",
    relatedConfigs: ["min.insync.replicas", "unclean.leader.election.enable"],
    failureModes: ["Total partition data loss on single-broker failure at replication factor 1"],
  },
  {
    key: "unclean.leader.election.enable",
    scope: "broker",
    goal: "Replication and durability",
    controls: "Whether an out-of-sync replica may be elected leader to restore availability.",
    defaultValue: "false",
    changeMechanism: "dynamic-cluster",
    riskOfChange: "high-risk",
    managedAvailability: "limited",
    whenToChange: "Enable temporarily only to restore availability when no in-sync replica is electable, knowing this can lose data.",
    performanceImpact: "No direct performance effect.",
    reliabilityImpact: "Enabling trades durability for availability; can commit an out-of-sync log as truth.",
    relatedConfigs: ["min.insync.replicas", "default.replication.factor"],
    failureModes: ["Silent data loss and divergent history when enabled during an outage"],
  },
  {
    key: "num.partitions",
    scope: "broker",
    goal: "Segment management",
    controls: "Default partition count for auto-created topics.",
    defaultValue: "1",
    changeMechanism: "dynamic-cluster",
    riskOfChange: "caution",
    managedAvailability: "full",
    whenToChange: "Size explicitly per topic based on target throughput and consumer parallelism, not the cluster default.",
    performanceImpact: "More partitions increase parallelism but add per-partition overhead on brokers and clients.",
    reliabilityImpact: "Partition count cannot be decreased later without recreating the topic.",
    relatedConfigs: ["default.replication.factor"],
    failureModes: ["Hot partitions from too few partitions relative to key cardinality"],
  },
  {
    key: "advertised.listeners",
    scope: "broker",
    goal: "Listener configuration",
    controls: "The address clients use to connect to a broker after the initial bootstrap.",
    defaultValue: "unset (must be configured)",
    changeMechanism: "broker-restart",
    riskOfChange: "high-risk",
    managedAvailability: "unavailable",
    whenToChange: "Configure explicitly whenever brokers are reachable via a different address than they bind to (containers, cloud NAT).",
    performanceImpact: "No direct performance effect.",
    reliabilityImpact: "Misconfiguration causes clients to connect successfully then fail on every subsequent request.",
    relatedConfigs: ["listeners", "listener.security.protocol.map"],
    failureModes: ["Producers/consumers can bootstrap but then time out reaching the real broker address"],
  },
];

export const configGoals = Array.from(new Set(configs.map((c) => c.goal)));
export const configScopes = Array.from(new Set(configs.map((c) => c.scope)));
